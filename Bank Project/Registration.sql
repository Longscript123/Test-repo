CREATE TABLE bank_of_trust.registration (
    first_name VARCHAR (50) NOT NULL,
    last_name VARCHAR (50) NOT NULL,
    mobile VARCHAR (50) NOT NULL,
    date_of_birth VARCHAR (50) NOT NULL,
    id_proof VARCHAR (50) NOT NULL
);